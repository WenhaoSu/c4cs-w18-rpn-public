language: python
python:
  - "3.5"
install:
  - pip install coverage
  - pip install python-coveralls
script:
  - coverage run -m unittest test_rpn.py
after_success:
  - coverage report
  - coveralls
